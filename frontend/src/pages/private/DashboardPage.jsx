// TODO — Phase 2, Step 5: fetch applications, build a pie chart (status breakdown) with Recharts
// TODO — funnel extra: compute conversion % Applied -> Interview -> Offer and show alongside the charts
export default function DashboardPage() {
  return <div className="p-6">Dashboard — TODO</div>;
}
