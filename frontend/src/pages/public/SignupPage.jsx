// TODO — Phase 2: form with name/email/password using <Input>, calls api/auth.js signup(),
// on success call useAuth().login(token) and navigate('/dashboard')
export default function SignupPage() {
  return <div className="max-w-md mx-auto py-16 px-4">Signup page — TODO</div>;
}
