// TODO — Phase 2: form with email/password using <Input>, calls api/auth.js login(),
// on success call useAuth().login(token) and navigate('/dashboard')
export default function LoginPage() {
  return <div className="max-w-md mx-auto py-16 px-4">Login page — TODO</div>;
}
